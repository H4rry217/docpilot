/**
 * Reserved for future AgentScope integration.
 *
 * <p>The first AI module version keeps AgentScope out of the main build. A later adapter can bridge
 * {@code AiChatModel} to AgentScope's {@code Model} contract without changing the public registry
 * used by DocPilot services.
 */
package io.docpilot.ai.agentscope;
